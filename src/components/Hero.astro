<section class="hero-wrapper">
  <div class="bg-track">
    <div class="bg-color"></div>
    <div class="bg-image"></div>
  </div>
</section>

<style>
  /* --- HERO WRAPPER --- */
  .hero-wrapper { 
    position: relative; 
    z-index: 1; 
    width: 100%;
  }

  /* --- BACKGROUND COLOR --- */
  .bg-color { 
    height: 100vh; 
    height: 100svh; 
    background-color: #0a0a0a; 
  }
  
  /* --- BACKGROUND IMAGE --- */
  .bg-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    height: 100svh;
    background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('/hero-robotica.jpg'); 
    background-size: cover;
    background-position: center;
  }
</style>