---
const trophies = [
  {
    slug: 'mundial-turquia-2023',
    year: '2023',
    title: 'Mundial Turquía',
    desc: 'Representamos a Salta en la instancia más exigente del planeta.',
    size: 'bento-big', 
    img: 'https://images.unsplash.com/photo-1531297484001-80022131f5a1?q=80&w=1000'
  },
  {
    slug: 'nacional-argentina-2024',
    year: '2024',
    title: 'Nacional Argentina',
    desc: '2do Puesto en la categoría Senior.',
    size: 'bento-wide',
    img: 'https://images.unsplash.com/photo-1485827404703-89b55fcc595e?q=80&w=800'
  },
  {
    slug: 'regional-salta-2022',
    year: '2022',
    title: 'Regional Salta',
    desc: 'Campeones invictos con puntaje perfecto.',
    size: 'bento-wide',
    img: 'https://images.unsplash.com/photo-1581092160562-40aa08e78837?q=80&w=800'
  }
];
---

<section class="wro-section" id="wro">
  <div class="wro-limit">
    <header class="wro-intro">
      <span class="wro-badge">ÉLITE MUNDIAL</span>
      <h2>World Robot Olympiad</h2>
      <p>Nuestra participación en la WRO es el reflejo de la excelencia. No solo competimos; resolvemos problemas reales bajo presión global.</p>
    </header>

    <div class="bento-container">
      {trophies.map(t => (
        <a href={`/wro/${t.slug}`} class={`bento-box ${t.size}`} style={`background-image: url(${t.img})`}>
          <div class="bento-gradient"></div>
          <div class="bento-info">
            <span class="bento-tag">{t.year}</span>
            <h3>{t.title}</h3>
            <p>{t.desc}</p>
            <span class="read-more-btn">VER CRÓNICA →</span>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  /* Contenedor Principal con fondo blanco forzado */
  .wro-section {
    background-color: #ffffff !important;
    padding: 100px 0;
    width: 100%;
    display: block;
    color: #121212;
  }

  .wro-limit {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .wro-intro {
    margin-bottom: 50px;
    text-align: left;
  }

  .wro-badge {
    color: #d83135;
    font-weight: 900;
    letter-spacing: 2px;
    font-size: 0.9rem;
    display: block;
    margin-bottom: 10px;
  }

  .wro-intro h2 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    text-transform: uppercase;
    color: #121212;
    margin: 0;
  }

  .wro-intro p {
    font-size: 1.1rem;
    color: #444;
    max-width: 650px;
    margin-top: 15px;
    line-height: 1.6;
  }

  /* SISTEMA BENTO GRID */
  .bento-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    width: 100%;
  }

  /* Reset de Enlaces (Anchors) */
  .bento-box {
    position: relative;
    background-size: cover;
    background-position: center;
    background-color: #1a1a1a;
    min-height: 300px;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
    text-decoration: none !important; /* Elimina subrayado azul */
    color: white !important; /* Elimina color azul de link */
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  /* Configuración de tamaños Bento */
  .bento-big { 
    grid-row: span 2; 
    min-height: 620px; 
  }
  
  .bento-wide { 
    grid-column: span 1; 
  }

  /* Capa de color/degradado sobre la foto */
  .bento-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(0deg, rgba(139, 29, 32, 0.85) 0%, rgba(0,0,0,0.1) 100%);
    z-index: 1;
    transition: all 0.4s ease;
  }

  /* Contenedor de texto interno */
  .bento-info {
    position: relative;
    z-index: 2;
    padding: 35px;
    width: 100%;
    transform: translateY(25px);
    transition: transform 0.4s ease;
  }

  .bento-tag {
    background: #d83135;
    color: white;
    padding: 4px 10px;
    font-weight: 900;
    font-size: 0.75rem;
    display: inline-block;
    margin-bottom: 12px;
  }

  .bento-info h3 {
    color: #ffffff !important;
    font-size: 2.2rem;
    font-weight: 900;
    text-transform: uppercase;
    margin: 0;
    line-height: 1.1;
    text-decoration: none !important;
  }

  .bento-info p {
    color: rgba(255,255,255,0.9);
    margin: 12px 0 20px;
    font-size: 1.05rem;
    line-height: 1.4;
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  /* Estilización del botón VER CRÓNICA */
  .read-more-btn {
    display: inline-block;
    color: #ffffff !important;
    font-weight: 900;
    font-size: 0.8rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    text-decoration: none !important;
    border-bottom: 2px solid #d83135;
    padding-bottom: 4px;
    opacity: 0;
    transition: all 0.4s ease;
  }

  /* INTERACCIONES (HOVER) */
  .bento-box:hover {
    transform: scale(0.985);
  }

  .bento-box:hover .bento-gradient {
    /* Al pasar el mouse, oscurecemos un poco más para que el texto resalte */
    background: linear-gradient(0deg, rgba(18, 18, 18, 0.95) 0%, rgba(0,0,0,0.3) 100%);
  }

  .bento-box:hover .bento-info {
    transform: translateY(0);
  }

  .bento-box:hover p, 
  .bento-box:hover .read-more-btn {
    opacity: 1;
  }

  .bento-box:hover .read-more-btn {
    color: #d83135 !important; /* El texto del botón cambia a rojo */
  }

  /* RESPONSIVE */
  @media (max-width: 768px) {
    .wro-section { padding: 60px 0; }
    .bento-container { grid-template-columns: 1fr; }
    .bento-big { grid-row: span 1; min-height: 400px; }
    .bento-info { transform: translateY(0); padding: 25px; }
    .bento-info p, .read-more-btn { opacity: 1; }
    .bento-info h3 { font-size: 1.8rem; }
  }
</style>