---
---

<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Taller de Robótica - Colegio Belgrano</title>
</head>
<body>
    
    <header id="navbar" class="hero-header">
        <div class="header-content">
            <h1 class="main-title">TALLER DE <span class="line-break"><br/></span> ROBÓTICA</h1>
            <div class="subtitle-container">
                <span class="subtitle">Colegio Belgrano</span>
                <img src="/logo-belgrano.png" alt="Logo Colegio Belgrano" class="logo-img" />
            </div>
        </div>
    </header>

    <main>
        <section class="hero-wrapper">
            <div class="bg-track">
                <div class="bg-color"></div>
                <div class="bg-image"></div>
            </div>
        </section>

        <section class="content-section">
            <h2>Construí el futuro</h2>
            <p>Aquí irá la grilla con las actividades, módulos y horarios...</p>
        </section>
    </main>

</body>

<style>
    body {
        margin: 0;
        font-family: system-ui, -apple-system, sans-serif; 
        background-color: #0a0a0a; 
        color: white;
        overflow-x: hidden;
    }

    /* --- CABECERA --- */
    .hero-header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100px; /* Aumentamos de 80px a 100px para que respire más */
        display: flex;
        align-items: center;
        z-index: 100;
        transition: all 0.4s ease;
    }

    .header-content {
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        transition: none;
    }

    /* --- ESTADO SCROLLED (BARRA) --- */
    .hero-header.scrolled {
        background-color: rgba(10, 10, 10, 0.98);
        backdrop-filter: blur(15px);
        height: 90px; /* Un pelín más pequeña que la original pero cómoda */
        border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    .hero-header.scrolled .header-content {
        transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
        transform: translateY(0) !important; 
        flex-direction: column;
        align-items: flex-start;
        padding-left: 5%; /* Reducido para que en móviles no se pegue al medio */
    }

    /* --- LOGO (Corrección de achatamiento) --- */
    .logo-img {
        width: 60px;
        height: auto; /* CLAVE: El navegador calcula el alto real sin deformar */
        transition: all 0.4s ease;
    }

    .hero-header.scrolled .logo-img {
        width: 35px; /* Solo tocamos el ancho, el alto se ajusta solo */
    }

    /* --- TEXTOS --- */
    .main-title {
        font-size: clamp(2.5rem, 8vw, 7rem); /* Ajustado para pantallas pequeñas */
        font-weight: 900;
        line-height: 0.9;
        margin: 0;
        letter-spacing: -2px; 
        text-transform: uppercase;
        background: linear-gradient(135deg, #d83135 0%, #be2b2d 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-header.scrolled .main-title { 
        font-size: 1.4rem; 
        line-height: 1.1;
    }

    .subtitle-container {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-top: 1rem;
        transition: all 0.4s ease;
    }

    .hero-header.scrolled .subtitle-container {
        margin-top: 0.1rem;
        gap: 0.5rem;
    }

    .subtitle {
        font-size: clamp(0.9rem, 2.5vw, 1.5rem);
        font-weight: 300;
        letter-spacing: 2px;
        color: #e0e0e0;
    }

    .hero-header.scrolled .subtitle { font-size: 0.8rem; }
    .hero-header.scrolled .line-break { display: none; }

    /* --- RESPONSIVE (Móviles) --- */
    @media (max-width: 768px) {
        .hero-header.scrolled .header-content {
            padding-left: 20px; /* Margen estándar en celulares */
        }
        
        .main-title { letter-spacing: -1px; }
        
        /* En móviles la barra puede ser un poquito más alta para facilitar el toque */
        .hero-header.scrolled { height: 85px; }
    }

    /* --- FONDOS Y CONTENIDO (Igual que antes) --- */
    .hero-wrapper { position: relative; }
    .bg-color { height: 100vh; background-color: #0a0a0a; }
    .bg-image {
        height: 100vh;
        background-image: url('https://images.unsplash.com/photo-1518770660439-4636190af475?q=80&w=2000'); 
        background-size: cover;
        background-position: center;
        box-shadow: inset 0 0 0 2000px rgba(0, 0, 0, 0.5); 
    }

    .content-section {
        min-height: 100vh;
        background-color: #ffffff;
        color: #111;
        padding: 5rem 1.5rem;
        text-align: center;
        border-radius: 30px 30px 0 0; 
        position: relative; 
        z-index: 10;
        box-shadow: 0 -20px 50px rgba(0,0,0,0.5);
    }
</style>

<script>
    const header = document.getElementById('navbar');
    const content = header.querySelector('.header-content');

    window.addEventListener('scroll', () => {
        const scrollY = window.scrollY;
        const vh = window.innerHeight;
        
        const startRising = vh; 
        const endRising = vh * 1.5; 
        
        if (scrollY < startRising) {
            header.classList.remove('scrolled');
            // Ajustamos el centrado para que considere la nueva altura de 100px
            content.style.transform = `translateY(calc(50vh - 50px))`;
            
        } else if (scrollY >= startRising && scrollY <= endRising) {
            header.classList.remove('scrolled');
            const distance = endRising - startRising;
            const progress = (scrollY - startRising) / distance;
            const currentY = (50 * (1 - progress));
            content.style.transform = `translateY(calc(${currentY}vh - ${50 * (1 - progress)}px))`;
            
        } else {
            header.classList.add('scrolled');
        }
    });
</script>
</html>